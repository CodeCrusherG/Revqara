# `scripts/`

Standalone `tsx` scripts.

- `seed-demo.ts` (Phase 4) — 12 vertical demo workspaces + `whatsapp_accounts`
  (known `phoneNumberId`) + bots. `npm run seed:demo`.
- `seed-razorpay-plans.ts` (Phase 7) — create the 4 paid Razorpay plans, print
  the `rzp_plan_…` ids for env.
